# Generate secure random values: python -c "import secrets; print(secrets.token_hex(32))"

HMAC_SECRET=changeme

KDC_SECRET_KEY=changeme
HR_SECRET_KEY=changeme
FINANCE_SECRET_KEY=changeme
IT_SECRET_KEY=changeme
OPS_SECRET_KEY=changeme

#DEBUG_FAKE_IP=8.8.8.8

# Set to "true" in production (behind a TLS-terminating reverse proxy)
REQUIRE_HTTPS=false
